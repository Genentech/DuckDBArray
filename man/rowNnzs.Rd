% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rowNnzs.R
\name{rowNnzs}
\alias{rowNnzs}
\alias{colNnzs}
\alias{rowNnzs,ANY-method}
\alias{rowNnzs,DuckDBArray-method}
\alias{colNnzs,ANY-method}
\alias{colNnzs,DuckDBArray-method}
\title{Row and Column Non-Zero Counts}
\usage{
rowNnzs(x, value = vector(type(x), 1L), ...)

colNnzs(x, value = vector(type(x), 1L), ...)

\S4method{rowNnzs}{ANY}(x, value = vector(type(x), 1L), ...)

\S4method{rowNnzs}{DuckDBArray}(
  x,
  value = vector(type(x), 1L),
  na.rm = FALSE,
  dims = 1,
  ...,
  useNames = TRUE
)

\S4method{colNnzs}{ANY}(x, value = vector(type(x), 1L), ...)

\S4method{colNnzs}{DuckDBArray}(
  x,
  value = vector(type(x), 1L),
  na.rm = FALSE,
  dims = 1,
  ...,
  useNames = TRUE
)
}
\arguments{
\item{x}{A matrix-like object.}

\item{value}{The value to treat as "zero" (i.e., elements NOT equal to this
value are counted). Defaults to the zero value for the storage mode of
\code{x}.}

\item{...}{Additional arguments passed to \code{\link[MatrixGenerics]{rowCounts}}
or \code{\link[MatrixGenerics]{colCounts}}.}

\item{na.rm}{If \code{TRUE}, missing values are excluded first.}

\item{dims}{A single integer specifying the dimension(s) over which to
operate. For \code{rowNnzs}, \code{dims = 1} counts non-zeros in each row.
For \code{colNnzs}, \code{dims = 1} counts non-zeros in each column.
Only used for \code{DuckDBArray} objects.}

\item{useNames}{If \code{TRUE}, the names of the input are preserved in
the output.}
}
\value{
An integer vector with length equal to \code{nrow(x)} for
  \code{rowNnzs} or \code{ncol(x)} for \code{colNnzs}.
}
\description{
Count the number of non-zero elements per row or column of a matrix-like
object. This is useful for filtering genes by detection rate (number of
cells where the gene is expressed) or filtering cells by complexity
(number of genes detected).
}
\details{
These functions are convenience wrappers around \code{rowCounts} and
\code{colCounts} that count elements not equal to zero. The zero value
is determined by the storage mode of \code{x}:
\itemize{
  \item For logical matrices: \code{FALSE}
  \item For integer matrices: \code{0L}
  \item For numeric matrices: \code{0}
  \item For character matrices: \code{""}
}
}
\examples{
# Dense matrix
mat <- matrix(c(0L, 1L, 2L, 0L, 0L, 3L), nrow = 2)
rowNnzs(mat)
colNnzs(mat)

}
\seealso{
\code{\link[MatrixGenerics]{rowCounts}} for counting specific values.
}
\author{
Patrick Aboyoun
}
